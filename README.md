# 🎮 VR ZONE - Booking System

Веб-приложение для бронирования сеансов в VR клубе с интеграцией Telegram бота для управления записями.

## ✨ Возможности

### 🌐 Веб-сайт
- 📱 Отзывчивый дизайн (мобильный, планшет, ПК)
- 🎨 Киберпанк стиль оформления
- 📋 Страницы: Главная, Игры, Цены, О нас
- 📝 Форма бронирования с модальным окном подтверждения
- ✅ Валидация данных (имя, телефон, дата)

### 🤖 Telegram Бот
- **`/list`** или **`/ls`** - список последних 50 записей
- **`/report`** - красивый отчет всех бронирований
- **`/update <id> <field> <value>`** - обновить запись
  - Пример: `/update 1 duration 30min`
- **`/delete <id>`** - удалить запись
  - Пример: `/delete 1`

⚠️ **Все команды доступны только администратору** (проверка по CHAT_ID)

## 🚀 Запуск

### 1. Установка зависимостей
```bash
python -m venv venv
source venv/bin/activate  # на macOS/Linux
# или
venv\Scripts\activate  # на Windows

pip install -r requirements.txt
```

### 2. Конфигурация .env
Создайте файл `.env` в корневой папке:
```env
BOT_TOKEN=123456789:ABCdefGHIjklmnoPQRstuvwxyz1234567890
CHAT_ID=814779912
SECRET_KEY=ваш-секретный-ключ
```

Получить `BOT_TOKEN`:
1. Напишите боту `@BotFather` в Telegram
2. Команда `/newbot`
3. Следуйте инструкциям

Узнать свой `CHAT_ID`:
1. Напишите боту `@userinfobot` в Telegram
2. Он пришлет ваш ID

### 3. Запуск приложения
```bash
python app.py
```

Откройте браузер на `http://localhost:5000`

### Запуск отдельного Telegram бота
Если вы хотите запустить бот отдельно от веб-приложения (рекомендуется в продакшн/отладке), запустите:

```bash
python bot.py
```

Бот использует ту же базу `bookings.db` и те же переменные из `.env` (`BOT_TOKEN`, `CHAT_ID`).

## 📦 Структура проекта

```
NEW FLASK/
├── app.py                 # Основное приложение (Flask + Telegram)
├── requirements.txt       # Зависимости Python
├── .env                   # Конфигурация (скрыта в .gitignore)
├── .env.example           # Пример конфигурации
├── bookings.db           # SQLite база данных
├── static/
│   └── css/
│       └── style.css     # Киберпанк стили
└── templates/
    ├── base.html         # Базовый шаблон с модалью
    ├── index.html        # Главная страница
    ├── games.html        # Список игр
    ├── prices.html       # Прайс-лист
    └── about.html        # О компании
```

## 🔒 Безопасность

- ✅ `.env` файл добавлен в `.gitignore`
- ✅ Секреты не хранятся в коде
- ✅ Команды бота защищены проверкой CHAT_ID
- ✅ Валидация всех пользовательских данных

## 💻 Использование

### Как забронировать (на сайте)
1. Прокрутите вниз до раздела "Бронирование"
2. Заполните форму (имя, телефон, дата, время, длительность)
3. Нажмите "Забронировать"
4. ✅ Появится модальное окно с подтверждением
5. Вы получите сообщение в Telegram

### Как управлять записями (в Telegram)
```
/list              → Показать последние 50 записей
/ls                → То же самое

/report            → Красивый отчет всех бронирований

/update 5 time 14:30    → Изменить время записи #5 на 14:30
/update 3 duration 60   → Изменить длительность записи #3

/delete 2          → Удалить запись #2
```

## 🎯 Клавиатурные команды Telegram

Бот поддерживает следующие команды (введите `/` в чат):
- `/start` - начало
- `/list` - список записей  
- `/report` - отчет
- `/help` - справка

## 🐛 Troubleshooting

### Модаль не появляется после бронирования
1. Откройте DevTools (F12)
2. Перейдите на вкладку "Console"
3. Попробуйте забронировать снова
4. Посмотрите сообщения в консоли (они начинаются с `[DEBUG]`)

### Telegram бот не отвечает
- Проверьте, что `BOT_TOKEN` правильный в `.env`
- Проверьте, что `CHAT_ID` совпадает с вашим ID
- Перезапустите приложение: `Ctrl+C` и снова `python app.py`

### Port 5000 already in use
```bash
# на macOS/Linux:
lsof -ti:5000 | xargs kill -9

# затем запустите снова:
python app.py
```

## 📊 База данных

Таблица `booking` содержит:
- `id` - уникальный идентификатор
- `name` - имя клиента
- `phone` - номер телефона
- `date` - дата бронирования (YYYY-MM-DD)
- `time` - время (HH:MM)
- `duration` - длительность (например: "30min", "1hour")
- `created_at` - время создания записи

## 🛠️ Технологии

- **Flask 3.1.2** - веб-фреймворк
- **Flask-SQLAlchemy** - ORM
- **aiogram 3.24.0** - Telegram Bot API
- **SQLite** - база данных
- **Python 3.14** - язык программирования

## 📝 Лицензия

Проект создан для VR ZONE club.

## 👨‍💻 Разработка

Последнее обновление: 6 января 2026

### Что было добавлено:
- ✅ Telegram бот с командами (/list, /report, /update, /delete)
- ✅ Модальное окно подтверждения после бронирования  
- ✅ Красивый отчет в Telegram
- ✅ Защита .env файла от утечки
- ✅ Debug логирование в браузере (F12 → Console)
# vr-zoneNEW
